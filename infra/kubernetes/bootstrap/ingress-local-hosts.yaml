# Ingress resources com HTTPS + hosts .local para desenvolvimento
# Certificados TLS emitidos pela CA interna via cert-manager
#
# Para funcionar, adicionar ao /etc/hosts do host:
#   echo '172.19.0.100  velya.local grafana.velya.local argocd.velya.local prometheus.velya.local patient-flow.velya.local discharge.velya.local task-inbox.velya.local ai-gateway.velya.local audit.velya.local agents.velya.local policy-engine.velya.local memory-service.velya.local decision-log.velya.local' | sudo tee -a /etc/hosts

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: velya-web-local
  namespace: velya-dev-web
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - velya.local
      secretName: velya-local-tls
  rules:
    - host: velya.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: velya-web
                port:
                  number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana-local
  namespace: velya-dev-observability
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - grafana.velya.local
      secretName: velya-local-tls
  rules:
    - host: grafana.velya.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: prometheus-grafana
                port:
                  number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd-local
  namespace: argocd
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - argocd.velya.local
      secretName: velya-local-tls
  rules:
    - host: argocd.velya.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: argocd-server
                port:
                  number: 443
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: patient-flow-local
  namespace: velya-dev-core
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - patient-flow.velya.local
        - discharge.velya.local
        - task-inbox.velya.local
        - audit.velya.local
      secretName: velya-local-tls
  rules:
    - host: patient-flow.velya.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: patient-flow
                port:
                  number: 80
    - host: discharge.velya.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: discharge-orchestrator
                port:
                  number: 80
    - host: task-inbox.velya.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: task-inbox
                port:
                  number: 80
    - host: audit.velya.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: audit-service
                port:
                  number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: platform-local
  namespace: velya-dev-platform
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - ai-gateway.velya.local
        - policy-engine.velya.local
        - memory-service.velya.local
        - decision-log.velya.local
      secretName: velya-local-tls
  rules:
    - host: ai-gateway.velya.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ai-gateway
                port:
                  number: 80
    - host: policy-engine.velya.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: policy-engine
                port:
                  number: 80
    - host: memory-service.velya.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: memory-service
                port:
                  number: 80
    - host: decision-log.velya.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: decision-log-service
                port:
                  number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agents-local
  namespace: velya-dev-agents
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - agents.velya.local
      secretName: velya-local-tls
  rules:
    - host: agents.velya.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: agent-orchestrator
                port:
                  number: 80
