apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: velya-apps

resources:
  - ../../bootstrap/argocd/platform-apps.yaml

commonLabels:
  app.kubernetes.io/part-of: velya-platform
  app.kubernetes.io/managed-by: argocd
