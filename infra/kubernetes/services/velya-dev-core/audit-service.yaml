apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-service
  namespace: velya-dev-core
  labels:
    app: audit-service
    app.kubernetes.io/name: audit-service
    app.kubernetes.io/part-of: velya-platform
    velya.io/service: audit-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: audit-service
  template:
    metadata:
      labels:
        app: audit-service
        velya.io/service: audit-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3004"
        prometheus.io/path: "/metrics"
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
      containers:
        - name: audit-service
          image: velya-audit-service:0.1.0
          imagePullPolicy: Never
          ports:
            - containerPort: 3004
              name: http
          env:
            - name: PORT
              value: "3004"
            - name: SERVICE_NAME
              value: "audit-service"
            - name: SERVICE_VERSION
              value: "0.1.0"
            - name: NODE_ENV
              value: "development"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /healthz
              port: 3004
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3004
            initialDelaySeconds: 15
            periodSeconds: 20
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
---
apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: velya-dev-core
  labels:
    app: audit-service
spec:
  selector:
    app: audit-service
  ports:
    - name: http
      port: 3004
      targetPort: 3004
  type: ClusterIP
