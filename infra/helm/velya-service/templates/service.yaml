apiVersion: v1
kind: Service
metadata:
  name: {{ include "velya-service.fullname" . }}
  labels:
    {{- include "velya-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "velya-service.selectorLabels" . | nindent 4 }}
