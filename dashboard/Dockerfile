FROM node:22-bookworm-slim

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .
RUN npm run build && npm prune --omit=dev

ENV NODE_ENV=production
ENV PORT=3000
ENV OPENSQUAD_SNAPSHOT_MODE=github
EXPOSE 3000

CMD ["node", "server.js"]
